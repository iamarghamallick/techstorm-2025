import { poppins } from "../fonts";

export default function Schedule() {
    return (
        <main className="schedule-container min-h-screen flex justify-center p-2 md:p-8 py-8">
            <div className="container">
                <h1 className={`${poppins.className} text-4xl text-white font-bold`}>Schedule - Coming Soon</h1>
            </div>
        </main>
    )
}